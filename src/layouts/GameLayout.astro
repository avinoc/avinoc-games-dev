---
import BaseLayout from "./BaseLayout.astro";
import SiteHeader from "../components/shared/SiteHeader.astro";
import SiteFooter from "../components/shared/SiteFooter.astro";
import { getGameBySlug } from "../data/games";

interface Props {
  title?: string;
  description?: string;
  game: string;
  hideSubnav?: boolean;
}

const { title, description, game, hideSubnav = false } = Astro.props;
const gameData = getGameBySlug(game);
const gameTitle = gameData ? `${title || gameData.name} | Project Avinoc` : title || "Project Avinoc";
---

<BaseLayout title={gameTitle} description={description} game={game}>
  <SiteHeader slot="header" currentPath={Astro.url.pathname} hideSubnav={hideSubnav} />
  <slot />
  <SiteFooter slot="footer" />
</BaseLayout>
